#!/bin/bash
echof info "Running docker-entrypoint"
set -e

# include dot-files (if present) when coping into mounted volumes
shopt -s dotglob

## a nice tidy linecounter and echo response to keep things clean.
function change_count() {
	read LINECOUNT && echof info "There were $LINECOUNT changes made."
}

# if empty, copy apache2 configuration to volume
if [ ! "$(ls -A /etc/apache2)" ]; then
		echof info "apache2 configuration dir was empty installing alternative."
		echof run "cp -r /usr/local/etc/apache2/* /etc/apache2"
		cp -r /usr/local/etc/apache2/* /etc/apache2
		echof info "copied apache2 configuration directory"
fi

# if Concrete5 is not yet installed, copy it into web root.
# This could be updated to be part of an upgrade mechanism.
echof run "ls /var/www/html/"
ls /var/www/html/
if [ ! -e '/var/www/html/config/concrete.php' ]; then
	echof warn "Concrete5 doesn't appear to be installed yet."
	echof blank "Don't worry this is normal first time around."
	if [ "$(ls -A /var/www/html)" ]; then
		echof warn "/var/www/html/ is not Empty. It contains:" 1>&2
		ls -A 1>&2
		echof info "Making an attempt to nicely merge files using."
		echof run "mv -fnu /usr/local/src/concrete5-$C5_VERSION/* /var/www/html/"
		mv -fnu /usr/local/src/concrete5-$C5_VERSION/* /var/www/html/
	else
		echof run "mv /usr/local/src/concrete5-$C5_VERSION/* /var/www/html/"
		mv /usr/local/src/concrete5-$C5_VERSION/* /var/www/html/
	fi
	echo "Concrete5 installed into /var/www/html"
	# change required permissions incl. for multi-lingual sites
	test_for_dir /var/www/html/application/files/ 775 "root:www-data"
	test_for_dir /var/www/html/application/config/ 775 "root:www-data"
	test_for_dir /var/www/html/packages/ 775 "root:www-data"
	test_for_dir /var/www/html/updates/ 775 "root:www-data"
	test_for_dir /var/www/html/application/languages/ 775 "root:www-data"
	test_for_dir /var/www/html/application/languages/site/ 775 "root:www-data"
	echof info "Copied Concrete5 and set permissions"
fi

echof run 'test_for_dir /var/www/html/packages 775 "root:www-data"'
test_for_dir /var/www/html/packages 775 "root:www-data"
echof run 'test_for_dir /var/www/html/application/config 775 "root:www-data"'
test_for_dir /var/www/html/application/config 775 "root:www-data"

# else default to run whatever the user wanted like "bash"
exec "$@"
